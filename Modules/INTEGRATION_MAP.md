# Titan Money Integration Map

Invoice issued → automatic invoice send → accounting journal/GST/receivables update → late follow-up when overdue.

Optional ZeroPay handoff is a read-only invoice payload only; ZeroPay owns payment sessions, PayID, QR, card, cash, webhooks, fees, and settlement.
