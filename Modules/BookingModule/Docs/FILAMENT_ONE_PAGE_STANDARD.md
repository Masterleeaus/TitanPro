# Filament Operator Standard

Filament resources and pages are optional operator surfaces. They must call Actions, Services, Queries and Policies and must not duplicate booking, scheduling, invoice, dispatch or reminder logic.
