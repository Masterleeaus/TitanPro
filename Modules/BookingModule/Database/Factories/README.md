# Factories

Legacy entities reference `Modules\BookingModule\Database\factories` (lowercase namespace). The concrete factory classes live in `Database/factories/` to preserve existing code. This directory is retained for Titan blueprint discovery.
