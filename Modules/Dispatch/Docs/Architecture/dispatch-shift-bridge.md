# Dispatch Shift Architecture

Dispatch owns shift templates, shift assignments, work orders, appointments, routes, route stops, and status logs. External modules should integrate through contracts/events rather than direct model dependencies.
