import ListModules from './ListModules'
import EditModule from './EditModule'

const Pages = {
    ListModules: Object.assign(ListModules, ListModules),
    EditModule: Object.assign(EditModule, EditModule),
}

export default Pages