# POST_STEPS

1) Upload/unzip this extension into your MagicAI extensions directory (same as usual).
2) In admin, ensure the extension is enabled.
3) Visit: `dashboard/user/social-media/agent/jobs` (auth required) to confirm the placeholder page loads.

No DB migrations are introduced in Pass 0 beyond what the extension already had.
