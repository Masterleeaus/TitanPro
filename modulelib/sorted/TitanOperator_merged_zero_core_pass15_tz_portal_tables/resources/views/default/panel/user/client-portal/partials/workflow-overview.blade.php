<x-card class="mb-0">
    <div class="px-5 py-5">
        <h3 class="mb-4 font-heading text-lg font-semibold">{{ __('Workflow posture') }}</h3>
        <div class="space-y-3">
            <div class="rounded-2xl border px-4 py-3">
                <p class="mb-1 font-medium">{{ __('Signal runtime') }}</p>
                <p class="mb-0 text-2xs text-heading-foreground/70">{{ __('Client portal remains on TitanOperator data while surfacing TitanZero runtime readiness for the app shell.') }}</p>
            </div>
            <div class="rounded-2xl border px-4 py-3">
                <p class="mb-1 font-medium">{{ __('Training source') }}</p>
                <p class="mb-0 text-2xs text-heading-foreground/70">{{ __('Use website, PDF, Q&A, and text steps from the existing chatbot builder.') }}</p>
            </div>
            <div class="rounded-2xl border px-4 py-3">
                <p class="mb-1 font-medium">{{ __('Execution style') }}</p>
                <p class="mb-0 text-2xs text-heading-foreground/70">{{ __('Best used as a client-facing portal with installable PWA shell and embedded chat.') }}</p>
            </div>
        </div>
    </div>
</x-card>
