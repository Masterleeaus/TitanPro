# Titan BOS Hub System

Each hub now has its own manifest, KPI map, action map, tab map, assistant roster, and lifecycle map.
The dashboard blades can progressively consume these files instead of hardcoding hub text.
