Dashboard finish notes:
- The three cards are now finishable from this pack.
- Updating Titan Zero improves the dashboard only where these cards consume shared runtime components/services.
- If you change card-specific markup or dashboard-only layout, you must update both.
- Best long-term path: keep dashboard cards thin and push shared logic into TitanRuntime.
