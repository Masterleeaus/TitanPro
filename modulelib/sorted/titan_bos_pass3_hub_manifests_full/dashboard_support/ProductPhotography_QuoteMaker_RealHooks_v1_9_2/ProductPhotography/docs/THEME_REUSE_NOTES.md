This pass removes the standalone page/card approach and repurposes the existing dashboard theme system.

Reused existing system features:
- panel.layout.app
- titlebar sections
- x-card
- x-button
- x-forms.input for text/select/textarea
- existing row/col layout utilities

No new UI component system was introduced in this pass.
Only QuoteMaker blades were edited to fit the existing panel patterns.
